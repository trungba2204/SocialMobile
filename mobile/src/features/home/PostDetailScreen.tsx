import { ScreenContainer } from '@/components/ScreenContainer';
import { Text } from '@/components/Text';

export function PostDetailScreen() {
  return (
    <ScreenContainer testID="post-detail-screen" padded>
      <Text variant="heading">Post</Text>
    </ScreenContainer>
  );
}
